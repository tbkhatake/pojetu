const soma = (a, b) => a + b /*A seta azul está substituindo o function e dessa forma o return está implicito*/

console.log(soma(3, 9))

const printf = (a) => console.log(a)/*console.log dentro da função para criar a função printf do c++*/

printf("Só para usar como se fosse em c++")